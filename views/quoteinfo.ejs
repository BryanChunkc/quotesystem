<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      h1,
      h2 {
        text-align: center;
      }
    </style>
    <title>Quote's info</title>
  </head>
  <body>
    <a href="/quotes" style="font-size: small">Back</a>
    <% for(var i = 0; i < quotes.length; i++) {%> <% if (quotes[i]._id == id)
    {%>
    <h1><%= quotes[i].quote %></h1>
    <h2><%= quotes[i].name %></h2>
    <% } %> <% } %>
  </body>
</html>
